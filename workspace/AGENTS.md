# Agent entry

Read START_HERE.md in this directory before acting. This directory is the AAA workspace manager: run AAA `:start`, use the shared TOOLBOX, and create projects only through the `start a project` flow. A nearest START_BRANCH.md takes precedence when working inside a branch. Never execute commands in the source template folders; copy and personalize them first.
