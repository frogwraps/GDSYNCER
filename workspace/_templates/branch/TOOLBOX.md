# Resources — {{BRANCH_NAME}}

Parent resources, inherited read-only: [parent TOOLBOX](../../TOOLBOX.md).
Branch rules: [START_BRANCH](START_BRANCH.md).
Shared services are reached through the parent TOOLBOX's workspace link.

## Branch resources and skills
None added. Record only this branch's actual additions and their access/verification recipes. No credentials.

## Optional memory
Disabled unless configured. Use distinct branch room/topic and vault-note path; never write this branch's full content into parent memory.
